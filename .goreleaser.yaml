# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: sparktype

before:
  hooks:
    - go mod tidy

builds:
  - id: sparktype
    main: ./cmd/sparktype
    binary: sparktype
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE*
      - README*

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'

release:
  github:
    owner: hntrl
    name: sparktype
  prerelease: auto
  draft: false

# Homebrew tap
brews:
  - name: sparktype
    repository:
      owner: hntrl
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/hntrl/sparktype
    description: Generate static types from OpenAPI specifications
    license: MIT
    test: |
      system "#{bin}/sparktype", "--help"
    install: |
      bin.install "sparktype"

# Publishers
publishers:
  # Publish platform-specific npm packages (runs once per archive)
  - name: npm-platform
    ids:
      - default
    cmd: node ./scripts/publish-npm-platform.js {{ .Version }} {{ .ArtifactName }}
    env:
      - NPM_TOKEN={{ .Env.NPM_TOKEN }}
      - NODE_AUTH_TOKEN={{ .Env.NPM_TOKEN }}

  # Publish main npm package (runs once, triggered by checksum file)
  - name: npm-main
    ids: []
    checksum: true
    cmd: node ./scripts/publish-npm-main.js {{ .Version }}
    env:
      - NPM_TOKEN={{ .Env.NPM_TOKEN }}
      - NODE_AUTH_TOKEN={{ .Env.NPM_TOKEN }}

  # Build and publish PyPI wheels (runs once, triggered by checksum file)
  - name: pypi-packages
    ids: []
    checksum: true
    cmd: ./scripts/publish-pypi.sh {{ .Version }}
    env:
      - PYPI_TOKEN={{ .Env.PYPI_TOKEN }}
