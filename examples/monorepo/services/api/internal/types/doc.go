// Package types contains generated type definitions from sparktype.
//
// These types are shared with the frontend and worker services.
// Run `sparktype generate` from the monorepo root to regenerate.
package types
