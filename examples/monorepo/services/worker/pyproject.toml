[project]
name = "worker"
version = "1.0.0"
description = "Worker service using shared sparktype types"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "mypy>=1.13.0",
]

